**subdomain**

subfinder -d sitename.com -all -silent -o subfinder.txt

**amass**
sudo systemctl restart snapd.apparmor
sudo systemctl restart snapd
export PATH=$HOME/go/bin:$PATH
source ~/.zshrc
which amass

amass enum -d sitename.com -o amass.txt

**Flitter amass data**
cat amass_final.txt subfinder.txt | sort -u > subdomains.txt

**DNSX**
dnsx -d sitename.com \
-w /usr/share/wordlists/seclists/Discovery/DNS/subdomains-top1million-5000.txt \
| tee dnsx.txt

**Flitter All subdomain**

cat amass.txt dnsx.txt subfinder.txt | sort -u > final_subdomains.txt

**HTTPS**

export PATH=$HOME/go/bin:$PATH
source ~/.zshrc
which httpx

httpx -l final_subdomains.txt \
-status-code -location -title -tech-detect \
-o httpx.txt

**Port scan**

sudo nmap -sS -sV sitename.com
sudo naabu -host sitename.com -o naabu.txt

**Directory Bruteforcing**

**Feroxbuster**

feroxbuster -u https://sitename.com \
-w /usr/share/wordlists/seclists/Discovery/Web-Content/raft-small-directories.txt \
-t 100 \
--status-codes 200,301,403 \
-o ferox.txt

 **Gobuster**

gobuster dir -u https://sitename.com \
-w /usr/share/wordlists/seclists/Discovery/Web-Content/common.txt \
-b 403 \
-o gobuster.txt


**Firewall**

wafw00f sitename.com

**Bug Finding**

**nuclei**

nuclei -u sitename.com -o nuclei.txt

katana -u sitename.com -js -aff -o katana.txt



